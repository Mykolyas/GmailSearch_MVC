@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Gmail Login</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons+Round" />

    <style>
        html, body {
            height: 100%;
            margin: 0;
            padding: 0;
            overflow: hidden;
        }

        .page-header {
            height: 100vh;
            width: 100vw;
            background-image: url('/img/bg-login.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
        }

        .mask {
            position: absolute;
            inset: 0;
            background: rgba(0, 0, 0, 0.4);
            z-index: 1;
        }

        .container {
            position: relative;
            z-index: 2;
        }

        .glass-box {
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            border-radius: 1rem;
            padding: 3rem;
            box-shadow: 0 0 40px rgba(0, 0, 0, 0.15);
            color: #000;
        }

        .btn-google {
            background-color: white;
            color: #333;
            border: 1px solid #ccc;
            font-weight: 500;
            transition: 0.3s ease;
        }

            .btn-google:hover {
                background-color: #f1f1f1;
                border-color: #999;
            }
    </style>
</head>
<body>
    <div class="page-header">
        <span class="mask"></span>
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-12 col-md-10 col-lg-8">
                    <div class="glass-box text-center">
                        <h1 class="display-4 fw-bold">👋 Ласкаво просимо!</h1>
                        <p class="lead">Цей застосунок дозволяє шукати ваші листи у Gmail за ключовими словами або за датами.</p>
                        <form method="post" asp-action="Login" class="mt-4">
                            <button type="submit" class="btn btn-google btn-lg px-4">
                                <i class="material-icons-round align-middle me-1">login</i>Увійти через Google
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
